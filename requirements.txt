# Local parsing for the audit (no API). The agents run on your Claude Code subscription.
python-docx>=1.1        # native .docx text + tables (do NOT rasterize Word files)
PyMuPDF>=1.24           # born-digital .pdf text extraction
